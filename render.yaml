services:
  - type: web
    name: sudarshan-api
    runtime: python
    repo: https://github.com/CalculusGuy/SUDARSHAN
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
